---
name: 链路各级余量现状 (2026-08-05) — 三级不达标: 翻页 1.0× / 面板 0.88× / dec+cpy 1.39×
description: ⚠ **2026-08-26 总修订: 载荷口径整个换了(3-bit+fold_a 7.85MB), 解码/memcpy 两级因 PL 解码而作废, 面板级从 0.247× 变约 0.69×**; 口径推导(体帧率=2×转速)与'多切片收益为零'仍有效
type: project
---

# 🎯 2026-08-26 总修订: 下表的**载荷口径和面板那一级全部换了**

这篇写于 08-05 的口径是 **900 RPM / 1-bit / 720 片 / 8.85 MB / 内容 15 fps**。
08-20 ~ 08-25 之后现役是**另一套数**:

| | 本文口径 (08-05) | **现役 (08-25, commit `970edce`)** |
|---|---|---|
| 转速 | 900 RPM / 15 rps | **969 RPM / 16.15 rev/s / 圈 62 ms** |
| 色深 | 1-bit | **3-bit 行内 BCM** |
| 载荷 | 720 片双面 **8.85 MB** | **213 片 7.85 MB** (fold_a: 面A 71 + 面B 142) |
| 解码 | CPU, `dec+cpy 48 ms` | **PL 三引擎 55.5 ms**, CPU 不参与 |
| 内容帧率 | 15.00 fps | **10-12 fps**, `flip == rx`, drop 9 |
| **瓶颈** | dec+cpy / 面板 | **翻页相位** (`wait` 24-38 ms/帧) |

**逐级重判:**
- ~~PC 压缩 0.67×~~ —— 仍靠预压缩绕过, 没变。
- ~~板端解码 2.4× / memcpy 3.3× / dec+cpy 1.39×~~ ⇒ **整级作废**: 解码进了 PL,
  `staging→bank` 的 memcpy 也随之消失(commit `7efe755`)。CPU 侧只剩收包 + 回写压缩流。
- **面板那一级 0.247×** ⇒ 数字变了但**问题性质没变**。08-10 的 1335 Hz 已作废,
  现役 3-bit 是 **1583 Hz**([[project_pov3d_refresh_vs_rpm]] 08-20 由 R0x28 寄存器直读结案)
  ⇒ 16.15 rev/s 下每圈画得出 `1583/16.15 ≈ 98` 个角度, 而配置 **142 槽/面**
  ⇒ 面板级余量 ≈ **0.69×**【推算, 未上板验证】。**仍然不满足, 但从 0.247× 好了很多。**
- **链路** ⇒ 见下面新增的一节。

🔴 **本文那条最有价值的结论没变, 而且更强了**:
**面板不满足只掉角分辨率, 不掉帧率**; 而"多切片提角分辨率在现平台收益为零"依然成立 ——
现在只是把 360 换成了 142, **同一个道理**。

## 新增: 链路那一级的真实数字 (2026-08-26 实测比率 + 推算)

本文原写"链路 33.5 Mbps / 余量 3.5×"。按现役 7.85 MB 载荷重算,
且用今天离线实测的 lz4-HC9 比率(**跨 8× 区间, 高度依赖内容**):

| 比率 | 每帧 | @11 fps | |
|---|---|---|---|
| 12.3× (最坏, `frames_3bit_dual50`) | 638 kB | 7.0 MB/s | **56 Mbps** |
| 35.5× (`frames_bonsai_max`) | 221 kB | 2.4 MB/s | **19 Mbps** |

~~Zynq 现役实测收包上限 **24.7 MB/s ≈ 198 Mbps** ⇒ Zynq 侧余量仍很足(3.5× 以上)。~~
🔴 **2026-08-27 更正: 198 Mbps 是突发窗口速率, 不是持续吞吐**(峰值当持续)。
**持续只有 110-125 Mbps** ⇒ 对最坏 12.3× 压缩比的 **56 Mbps** 需求,
Zynq 侧余量是 **约 2×**, 不是 3.5×。见 [[reference_dr1_wifi_ceiling_unestablished]]。
🔴 **但 DR1 侧这一级可能是天花板** —— DR1 的 WiFi 吞吐从未确立,
见 [[reference_dr1_wifi_ceiling_unestablished]]。

---

2026-08-05 上板实测后的余量表。目标口径见下节。
上位: [[feedback_lz4_onboard_reality_check]](实测数据) / [[project_pov3d_final_target]](目标推导)。

## 🎯 2026-08-06 更新: **15 fps 已达成**, 下表两行作废

| 原判 | 现状 |
|---|---|
| 链路 3.7× ✅ | ⚠ **当时是错的** —— 真实收包只有 2.83 MB/s(0.4×), 因为板端自己设 SO_RCVBUF 关掉了内核接收窗放大。删掉后 24.7 MB/s = **3.5×** |
| 翻页 1.0× ❌ "相位没锁" | ✅ **前提被证伪并作废**。drop=0/668, 相位可回收量为 0; 修完收包后 **flip 中位 15.00/s** |

详见 [[feedback_recv_setsockopt_rcvbuf_lock]] / [[feedback_phase_lock_premise_wrong]]。
**现在的瓶颈是 flip 线程的 `cpy 26.1 ms` + wait, 以及下表仍然成立的"面板 0.88×"。**
下文其余部分(口径推导、面板 0.88×、多切片收益为零)全部仍然有效。

## 🔴 先定口径 (这一点反复搞混过)

```
体帧率 = 2 × 转速          (穿轴屏转半圈遍历所有平面方向)
内容帧率 ≤ 转速            (一圈只能翻一次页)

⇒ 体帧率 30Hz 只需 **900 RPM (15 rps)**, 内容帧率上限 = **15 fps**
```
🔴 **900 RPM 就达标, 不需要 1800。** 我一度建议 1800 RPM —— 那**只是为了给翻页那一级凑 2× 余量**,
不是达标需要。用户明确: **只能 900 RPM, 再高振动太大。**

⚠ 两个面**由同一次翻页更新** (一帧载荷同时含面A+面B 的切片), 不是两条独立 15Hz 流水线。

## 余量表 (900 RPM / 内容 15 fps / 周期 66.7 ms / fold540 + lz4-HC9 + 均衡三流)

| 级 | 需求 | 能力 | 余量 | |
|---|---|---|---|---|
| PC 压缩 lz4-HC9 | 66.7 ms/帧 | 99 ms/帧 | 0.67× | 预压缩可绕过 |
| ~~**链路**~~ | 33.5 Mbps | ~~125 Mbps~~ | ~~3.7×~~ | ⚠ **作废, 见文首** |
| **板端解码** | 28 ms | 66.7 ms | **2.4×** | ✅ |
| **memcpy** | 20 ms | 66.7 ms | **3.3×** | ✅ |
| **dec+cpy 串行链** | **48 ms** | 66.7 ms | **1.39×** | ⚠ |
| ~~**翻页**~~ | 每圈必须翻中 | — | ~~1.0×~~ | ✅ **已解, 见文首** |
| **面板显示** | 360 片/圈 | **316 片/圈** | **0.88×** | ❌ |
| 体帧率 | 30 Hz | 30 Hz | 1.0× | 几何自动 |

⇒ **会话开始时以为的两个瓶颈(链路、解码)现在都不是问题了**; 剩下三级不达标。

## 🔴 翻页 1.0× 的真因是**相位没锁**, 不是转速不够

`dec+cpy = 48 ms < 66.7 ms` **本来就够**。问题是解码启动相位相对转子随机漂移:

| 情况 | 结果 |
|---|---|
| 完成相位**对准**窗口 | 48 干活 + 18.7 等 = 66.7 ✅ 每圈翻中 |
| 相位**随机** | 48 + 平均等半圈 33 = 81 > 66.7 ❌ 两圈一帧 → **7.5 fps** |

**没有中间态: 翻中就 15 fps, 翻不中直接掉一半。** 实测 16.1 rps 下只翻中 ~2/3 窗口。

⇒ **解法是翻页后立刻开始解下一帧** (相位锁定), 纯软件。
⚠ 这同时解释了长期没查清的 "`wait` 平均 31 ms 为何没被 `window=2` 重叠掉" ——
**window 管的是在途帧数, 不管解码启动相位。**

## 🔴 面板 0.88× — 本轮才浮出来的一级, 之前的余量表里根本没有它

### 🎯 2026-08-10 板上实测定案: 面板一级是 **0.247×**, 每圈只显示 **89** 片

实测 2D 刷新 **1335 Hz**(不是 4748, 也不是按模型重算的 2393), 见
[[project_pov3d_refresh_vs_rpm]] §08-10 实测。

| | 账上写过 | 模型重算 | **实测** |
|---|---|---|---|
| 2D 刷新 | 4748 Hz | 2393 Hz | **1335 Hz** |
| 每圈显示切片 @15rps | 316 | 160 | **89** |
| 面板级余量 | 0.88× | 0.44× | **0.247×** |
| 有效角分辨率 | 1.14° | 2.26° | **4.04°** |

🔴 **现在渲的 360 片里有 3/4 根本没机会上屏**, 链路/解码/memcpy 为它们付的代价全白付。

🔴 **面板这级"不满足"与别的级不是一回事**:
- 别的级不满足 ⇒ **掉帧率**(内容 fps 下降)
- 面板不满足 ⇒ **掉角分辨率**, 帧率不受影响

内容仍是 15 fps 在更新, 只是每圈画得出 89 个角度而非 360。**这不是"通信速率不够",
是显示端画不过来** —— 链路那边其实在超量供货。

⇒ **立刻可做且零风险**: 把 `n_slices` 从 360 降到面板真能显示的量级,
链路/解码/memcpy 全线成比例减负, **画面质量一点不损失**(那些片本来就没上屏)。

⚠ 但"降级是 bit29 造成的"仍是**推断不是实测** —— 实测行周期比 RTL 模型长 1.79×,
模型已经错过一次。别在没做"翻 bit29 再测"那个对照实验之前, 把 2× 收益当成已知。

---

### ⚠ 2026-08-10 早些时候的模型重算 (已被上面的实测取代): 不是 0.88×, 是 0.44×

`pov_boot.sh:38` 的 bit29=1 ⇒ `ddr_slow=1` ⇒ 面板刷新其实是 **2393 Hz 不是 4748**
(位语义反转, 见 [[feedback_reg_bit_semantics_reversed]] / [[project_pov3d_refresh_vs_rpm]])。

| | 本节原写 | 实际 |
|---|---|---|
| 面板刷新 | 4748 Hz | **2393 Hz** |
| 每圈显示切片 @15rps | 316 | **160** |
| 面板级余量 | 0.88× | **0.44×** |
| 有效角分辨率 | 1.14° | **2.26°** |

⇒ 下文"要 360 片/圈需 1.14×"实际是 **2.26×**;
但 **翻 `B8→98` 一个字符就能拿回 2.0×**(占空比守恒 ⇒ 亮度不变, DDR 带宽也不变),
之后面板级从 0.44× 回到 0.88×。**这是全表里最便宜的一级。**
🔴 前提: 07-27 换 trans_v12 引脚图后 50 Mbps 没有点屏记录, 翻之前必须重测 SI。

⇒ 也意味着**现在渲 360 片是多传了 2.25× 的数据**(改 fast 后 1.14×)。
把 `n_slices` 匹配到面板真实能力, 链路/解码/memcpy 全线成比例减负 ——
`dec+cpy` 那个 1.39× 的余量能直接翻倍。

---

```
面板刷新 4748 Hz (oe=111, 已是免费亮度上限, 见 project_pov3d_refresh_vs_rpm)
900 RPM = 15 rps
⇒ 每圈能显示的**不同**切片 = 4748/15 = 316 片   而现在渲 360 片
```
**渲多少片都一样, 每圈只显示得了 316** —— 有效角分辨率被**面板刷新率**锁死在 1.14°,
不是被切片数锁死。

### 因此"多切片提角分辨率"在现平台上收益为零
用户提过一个正确的观察: 360 片 = 每片 1°, 转子真实角度与最近切片有 **±0.5° 量化误差**
(最外圈 75mm 处 = ±0.65mm ≈ ±0.7 像素); 切 720 片降到 ±0.25°。
**逻辑成立, 拐点也正好是我们算过的像素匹配数** (503 片/圈 @160px 屏 / 603 @192px 屏,
见 [[project_pov3d_final_target]])。

🔴 **但 900 RPM 下面板只显示 316 片, 切 360/503/720 有效角分辨率全是 1.14°**,
多切的部分根本没机会上屏, 而数据量/解码/链路的代价是实打实翻倍的。
```
要显示 720 片/圈 @15rps ⇒ 面板需 10,800 Hz = 现在的 2.3×
要显示 360 片/圈 @15rps ⇒ 面板需  5,400 Hz = 现在的 1.14×   ← 最小的一步
```
⇒ **想要更细角分辨率, 该投的是面板刷新率, 不是切片数。**

## 下一步优先级 (2026-08-05 定, ⚠ 第 1 条已被 08-06 结果取代)

1. ~~**相位锁定**~~ ❌ **前提是错的, 别再投时间** ([[feedback_phase_lock_premise_wrong]])。
   15fps@900RPM 是靠**修收包**拿到的 ([[feedback_recv_setsockopt_rcvbuf_lock]])。
   ⇒ 现在的第 1 优先级是 **flip 线程的 cpy 26.1 ms**(原第 3 条)
2. **面板刷新 4748 → 5400 Hz** —— 让渲的 360 片能完整显示 (PL 扫描拓扑)
3. **干掉 20 ms memcpy** —— dec+cpy 1.39× → ~2× (双核并行拷 / PL330 DMA / 三缓冲流水)
4. ICND2260 主线 (scatter 内核 / 屏驱 RTL)

⚠ **1-3 全是现平台(PVS1 切片流)的优化, 而 ICND2260 目标架构根本不传切片。**
投不投这些时间取决于是先把现有这套做到位, 还是直接推新架构。

## ⚠ 端到端数字的可信度问题 (建议先解)
WiFi 每 20-40 s 有 1-3 次 **300-840 ms 停顿**(环境性), 同一配置 povstream 总平均
能差 **7.49 vs 10.14**。**每配置只跑了一次。**
⇒ 后续任何优化都无法判断是否真的有效, **重复多次取分布这件事本身应该先做**。
板端 DIAG 的 rx/s 比 povstream 总平均可靠。
