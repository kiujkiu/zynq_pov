---
name: project_pov3d_lvds_b2b_bench
description: 🎯 2026-09-18 立项 — 两块待用 DR1V90(FS03) 板对板 LVDS 台架, 测 FS03 那 33Ω 串阻 + 96:2 排针地线在 800M/1G/1.2G 的眼宽; 🎯 关键结论: DR1↔DR1 用 LVDS18 **跳线就够, 不必做 2.5V LDO 改板**; 发端三档全 0 违例(1.2G Fmax 602.8 在过严口径下); 每块板只拆 4 只排阻(R188/195/196/197); 工程 dr1v90/lvds_b2b/
metadata:
  type: project
---

# lvds_b2b — 板对板 LVDS 眼宽台架 (2026-09-18 立项)

用户 09-18 问「33Ω 串阻对 1.2G LVDS 有没有影响」→「能不能用两块板测一下」→
「我焊接板子, 你准备代码」。工程在 **`dr1v90/lvds_b2b/`**, 操作手册是它的 `README.md`。

## 🎯 三条值钱的结论

### 1. DR1↔DR1 走 LVDS18, **跳线就够, 不必做 2.5V LDO 改板**

之前所有讨论都默认"要跑 LVDS 就得把 CEP bank 改 2.5V", 于是背上了
[[project_pov3d_hw_pack_dr1v90_gw5a]] §FS03改板 那一整套 LDO 模块 + 一堆烧板风险。
**那套只在要跟 GW5A 按 LVDS25 对接时才需要。** 两块 DR1 互连时:

| DS1200 V1.0 表 3-14 | 最小 | 典型 | 最大 |
|---|---|---|---|
| LVDS18 **输出** VOCM | 0.3 | **0.6** | 0.8 V |
| LVDS18 **输入** VICM (片内 100Ω) | 0.4 | **0.6** | 0.8 V |

典型值对齐, 而且表 3-21/3-22 里 **LVDS18 与 LVDS25 同为 1250 Mbps**。
⇒ J1/J2 跳帽拨到 1.8V 侧（出厂就有这一档）即可, 零改板。
⚠ 角对角有 0.1V 缺口(发 0.3 < 收 0.4), 典型值没问题, 极限角纸面差一点。
⚠ **必须用片内 100Ω**: 外接 100Ω 时输入 VICM 变 0.4/**0.9**/1.0, 典型值反而对不上 0.6。

### 2. 1.2 Gbps 在 DR1 发送端成立 —— "没有 8:1 SERDES"没有变成墙

线速率 = 4 × pclk（`ODDRx2` 是 4:1, [[project_pov3d_bridge_architecture]] 已确认 DR1 最高 gearing x2）。
6 lane 实测（DR1V90GEG484 -2）:

| pclk | 线速率 | SWNS | HWNS | Fmax | 违例端点 |
|---|---|---|---|---|---|
| 200 | 800 Mbps | +0.244 | +0.001 | 443.3 | 0 |
| 250 | 1000 Mbps | +0.007 | +0.001 | 501.8 | 0 |
| **300** | **1200 Mbps** | **+0.007** | **+0.005** | **602.8** | **0** |

🔴 **全部是 TD 的"过严口径"** —— 自推时钟没把 `DR1_PHY_LCLK` 的 ÷2 算进去,
把 pclk 域全部逻辑按 sclk 频率约束（1.2G 档按 1.666ns 而非真实 3.333ns）。
比真实严一倍还 0 违例 ⇒ fabric + IOL 这一段够。

📌 **一个会反复踩的坑**: 第一次跑 300MHz 出 21 个 setup + 1 个 hold 违例,
查下来最差那条是**我自己写的 26 位心跳计数器**的 19 级进位链, 与链路速率毫不相干,
被过严口径放大成头号违例。挪出 pclk 域后 setup/hold 全部归零。
⇒ **pclk 域里不要放任何与链路无关的逻辑**, 否则真违例被盖住。

### 3. 每块板只拆 4 只排阻

FS03 的 33Ω 是 `R187~R210` 共 24 只 RA4_0402（[[project_pov3d_fs03_cep_ground_and_third_row]] §5 已坐实）。
选脚时**按排阻分组挑**, 让要动的电阻最少:

| 用途 | CEP1 脚 | 球 | 排阻 |
|---|---|---|---|
| 转发时钟 | 7/8 | C17/C18 (GCLKIOT3/C3) | **R188** |
| lane 0~5 | 35~46 (连续 12 脚) | F18/D16/A21/A18/F16/E15 + N | **R195/R196/R197** |

数据 6 対全部取自 [[project_dr1_uplink_tx_probe]] 09-03 实跑过的那 14 対
（"合法 DDRx2 location"无任何公开文档, 只能试, TD Error-8221）, 一対都没新加。
⇒ 拆 4 只就能把整条链路 33Ω→0Ω。三种配置: A 两端原样(66Ω/腿) → B 收端拆(33Ω) → C 两端拆(0Ω)。

🔴 排阻尺寸: `RA4_0402` = **4 个 0402 元件, 整个封装 2.0×1.0mm**。
4×0603 的排阻是 3.2×1.6mm, 差一倍, 不会认错。

## 设计上几个非显然的取舍

- **转发时钟用 ODDR(x1) 不用 ODDRx2**: 时钟対必须落在 GCLK 対上（收端要进 PLL）,
  而 09-03 那 14 対**刻意避开了全部 GCLK 対**, GCLK 球是不是合法 DDRx2 location 没人试过。
  ⇒ 发端用 ODDR 出一个 pclk 频率方波, 对球位要求低得多。收端 PLL 拿它当 refclk
  （10~500MHz 合法, 表 3-18）, FBCLK_DIV=2 倍回 sclk。代价是时钟与数据有固定 skew, 无所谓（要扫相位）。
- **PRBS7 自同步检错, 不用训练码**: 期望值 = 收到的 `b[n-6]^b[n-7]`, 对**整体比特滑移免疫**
  ⇒ 不需要字对齐/训练期, 相位转过整个 UI 也不会错位报错, 扫描可以无脑连续走。
  代价: **误码倍增**(线上 1 个错报 3 次) ⇒ `err_cnt` 不能直接当 BER 分子。
- **扫相位用 PLL 动态细调, 不用 IDELAY**: 步长 = T_VCO/8 确定可算（1.2G 档 69.4ps, 12 点/UI）,
  量程无限。而 `DR1_LOGIC_DYNAMIC_IDELAY` 的 tap 步长**手册自相矛盾**
  （UG1210 写 215~1357ps/级, TD 模板写 170ps+23ps/tap, 差一个量级）⇒ 只配当 per-lane 微调, 且要先标定。
- **误码计数跨域用 req/ack 握手, 不是同步器**: 多位计数器逐位打同步器会**撕裂**。
  握手放在 `b2b_rx_top` §6a（那里同时拿得到两个时钟）, 不是放在寄存器块里。
- **`IDDRx2`/`ODDRx2` 的 pad 不能经 IBUFDIFF/OBUFDIFF**, 必须直连顶层端口,
  差分由 `.adc` 的 IOSTANDARD 自动配。插了撞 **SYN-8207 "can't be mapped into PAD"**（09-18 踩过）。

## 🔴 只能上板回答的四件事

1. 移相时 `DR1_PHY_LCLK` 的 ÷2 相位**会不会翻转**（手册没写）。翻了 `IDDRx2` 依赖的
   "sclk 下降沿对齐 pclk 边沿"就错, 症状是**整圈没有眼 (dead 全 1)**。上板第一件要看的。
2. `IDDRx2` 的 q0 是不是最早收到的比特（模板只写 "The 0th bit"）。错了症状是
   **"有眼但 PRBS 全错"**, 把 4 个 q 顺序倒过来即可。
3. sclk 600MHz 贴着 GCLK 网络上限 628MHz（表 3-16, -2 档）, 只剩 4.7%。
4. `ODDRx2` 内部 pclk→sclk 那一跳在硬 IOL 里, **TD 不暴露也不报**。

## 收端三档也过了 —— 但要看对指标

🔴 **别看 SWNS, 看"pclk 域 Min Period vs 真实 pclk 周期"。**
TD 第 1 趟自推的时钟把 pclk 域按 sclk 频率约束 ⇒ SWNS 是按 2× 真实需求算的, 负值不代表跑不了。
`build.sh` 的**判据 4** 现在自己算这个真实余量, 不用再手推:

| pclk | 线速率 | Min Period | 真实周期 | 真实余量 |
|---|---|---|---|---|
| 200 | 800 Mbps | 3.029 | 5.000 | **+39.4%** |
| 250 | 1000 Mbps | 3.142 | 4.000 | **+21.5%** |
| 300 | 1200 Mbps | 2.998 | 3.333 | **+10.1%** ⚠ 偏紧 |

📌 **高速时钟域里不要放长进位链** —— 这一条踩了两次:
发端 26 位心跳(19 级 ADDER) 让 1.2G 出 21 个 setup + 1 个 hold 违例;
收端 32 位误码累加器(33 级) 把 pclk 域 Min Period 顶到 3.295ns, 1.2G 要 3.333ns 几乎零余量。
两处都与链路速率毫不相干, 却决定该域 Fmax, **而且会把真违例盖住**。
修法: 心跳挪去 25MHz 域; 计数器改 **16 位分段**(段间用**同一拍**的进位条件, 保证 {hi,lo} 任意时刻自洽,
否则跨域快照拼出来的值不对)。桥片 RTL 同样适用。

## 状态

- ✅ 发端全套 + 三档 bit（`bits/b2b_tx_p{200,250,300}_n6.bit`）
- ✅ PRBS 收发 + iverilog 自测（`sim/run_sim.sh` → `SIM: PASS`, NLANE=1/5/13, 含变异测试）
- ✅ 收端顶层 + 扫眼 FSM + GP0 寄存器块 + 板端 `out/load_b2b.sh`（读数通路照抄 [[project_pov3d_jtag_load_bench]]）
- ✅ 收端三档综合通过, bit 已出; `build.sh rx` 还会顺手 bootgen 出 `out/b2b.bin`
- ✅ **2026-09-20**: 四角色 (tx / rx / conn_tx / conn_rx) **全部挂 `b2b_sys`** —— 起因是发端按"JTAG 烧 bit"漏挂从机, `load_b2b.sh` 读 MAGIC 把单核挂死、看门狗重启回生产位流; 教训见 [[feedback_every_pl_bitstream_needs_a_gp0_slave]]
- ✅ **2026-09-20 15:4x**: GP0 补齐后重编 **800M (`p200_n6`) 发/收** 出 bit+bin（`out/b2b_tx_p200_n6.bin` / `out/b2b_rx_p200_n6.bin`, MANIFEST 已更）; 发端 SWNS +0.348 / HWNS +0.004 / 0 违例; 收端 SWNS 仍是过严口径负值（看 Min Period 真余量, 同 §「收端三档也过了」）
- 🔴 1.2G 那档收端只剩 10.1% 是**工具层**余量, 上板还要再扣 sclk 600MHz 贴 GCLK 上限(628)
  与 IOL 内部那一跳(TD 不报) ⇒ 先从 800M 做起

## 🎯 2026-09-21 双板对插上板实测 —— 链路跑通, 定位并修掉一个真 bug

板子: DR1V90-1(COM5)=发端, DR1V90-2(COM8→实为 COM18)=收端, 都跑 TF 卡 Linux。
排线连通性(走马灯 conn_tx/conn_rx): **48/48 全通, 0 短路 0 错位**(重跑两轮)。

### 🔴 根因: 4:1 解串比特序反了 (BITREV 应为 0, 不是 1); 09-20 因**另一个 bug 遮蔽**而选反
- 现象: 800M 上板 6 条 lane 全 DEAD、整圈无眼; 但转发时钟対(C17/C18, ODDR/LVDS18)能让**收端 PLL 锁住**
  ⇒ 物理链路是通的。发端 lane0 用 ODDR 发方波(PROBE_LANE0=1)在收端也收到干净的 `11001100`
  ⇒ 基础 LVDS + IDDRx2 采样通; 挂的是全速率 ODDRx2 4:1 那条。
- 定位手段(关键): 把 `b2b_rx_top.v` §3b 那段**原来只写不读、被综合剪掉**的原始比特捕获 `cap[]`
  真接到 MAP 寄存器口(`map_ok` 从眼图改接 `cap_rd`), 并改成**扫眼结束后一次性冻结**(不再滚动重抓,
  否则串口逐个读 MAP 相隔 ~100ms、缓冲每 ~5ms 重抓 ⇒ 拼不出连续比特)。上板抓 64 个连续 bit,
  离线比对 PRBS7 m-序列: **把捕获流做 per-beat 反转后 lane3 吻合 100%、其余 94~98%** ⇒
  正确比特序是 **q0 当最早 (BITREV=0)**, 与 TD 模板注释 "The 0th bit" 字面一致。
- 为什么 09-20 选成 BITREV=1: 当时 `auto_start` 有 bug(pll_locked 查得太早, TLOCK 最大 250µs 还没锁,
  扫眼 FSM 根本没跑完), BITREV=0/1 **都假性全 DEAD** ⇒ 拿一个坏掉的判据(扫眼没跑)去比两种接法,
  两个都 fail 就选了错的那个。auto_start 修好(等 pll_locked 稳定后再延迟发 start)后才现原形。
  📌 **教训**: 判据本身坏掉(扫眼没运行)时, "两个候选都 fail" 不能用来选方向; 要先弄一个**直接看原始数据**
  的量具(抓真比特), 而不是信一个可能没在工作的 pass/fail。
- 修法: `b2b_rx_top.v` 参数 `BITREV` 默认 1→**0**。BITREV=0 后误码立降 10~100×。

### 实测结果 (BITREV=0, 配置 A 两端原样 33Ω = 每腿 66Ω, ≤10cm 直通排线)
| 线速率 | pclk | 结果 |
|---|---|---|
| **200 Mbps** | 50 | **lane1 / lane3 在 314M beats(~12.6 亿 bit)上持续 0 误码 (clean)**; lane2=396 / lane4=129 (近乎干净); lane5、lane0 差(lane0 判 DEAD) |
| 400 Mbps | 100 | 6 条全 DEAD, 但误码有 lane3<lane2<lane1<lane4<lane5<lane0 的**梯度**(lane3 最好), 原始比特离线验证仍是 PRBS |
| 800 Mbps | 200 | 6 条全 DEAD; err_cnt 全 ~1.2~1.9G(≈50%) 但那是 BITREV=1 旧 bit 的数; BITREV=0 未在 800M 重测 |

### 🔴 现在的墙: 单一全局采样相位 + 零误码 dwell 判据 + 逐 lane 偏斜
- 扫眼判据是"某 tap 上整个 dwell(2^16 psclk≈百万 bit)**零误码**"⇒ 要求 BER<~1e-6, 对边际链路太苛。
- 本版是**全局相位**(6 lane 共用一个采样点, 无 per-lane IDELAY 去偏斜, README §6.6 早写明)。
  逐 lane 偏斜下, 没有一个全局相位能让 6 条同时干净 ⇒ 200M 也只有 lane1/3 撞上、其余差。
- ⇒ 要往上做 400/800M 或让 6 条都干净, 下一步二选一(未做):
  ① 加 `DR1_LOGIC_DYNAMIC_IDELAY` per-lane 去偏斜(tap 步长手册自相矛盾, 要先标定, README §6.6);
  ② 把量具从"零误码眼宽"换成 **BER-vs-相位浴盆曲线**(边际链路本就该用浴盆, 不是零误码眼)。

## 🎯 2026-09-21 (下午) 加了 BER 浴盆量具, 测出眼宽-vs-速率曲线 (配置 A)

原来的扫眼判据是"整个 dwell 零误码 + 6 条共用一个采样相位", 太苛 + 撞逐 lane 偏斜 ⇒ 只会 clean/DEAD。
改成**运行时可调误码门限**: `b2b_regs` 新增 **THRESH 寄存器 @0x3C** (不能放 0x38 —— 那是自检
"EYE 区尾后一个字 0x20+4*NLANE" 的未映射探测点, 09-21 放 0x38 直接自检 FAIL);
`b2b_rx_top` 的 sticky 从"出过任何错"改成"脏 beat 数 > 门限"(每 lane 每极性 16 位饱和计数器,
thresh 同步进 pclk 域)。门限=0 时与原零误码判据完全等价。
MAP 口(找 bug 时借去当原始比特, 现已还原成眼图)每档每 lane 一位 ⇒ 就是**二值浴盆**,
主机侧从 MAP 算每条 lane 自己的最长干净档 = 眼宽(%UI)。板端 `bath.sh` 扫门限、`load_b2b.sh thresh N`。

### 眼宽-vs-速率实测 (配置 A: 两端各 33Ω=每腿 66Ω, ≤10cm 直通排线; 6 lane 全 PRBS)
| 线速率 | pclk | 结果 |
|---|---|---|
| **200 Mbps** | 50 | lane1/2/3/4 **零误码满眼**(扫 2 UI 全干净); lane5 零误码眼 **22%UI**(容 16 脏beat 就满); **lane0 任何相位任何门限都 DEAD** |
| 400 Mbps | 100 | 零误码全 DEAD; 门限放到 ~1~3% 只有 **lane3** 开(FLAGS=0x3700, MAP=0x08); 其余仍死 |
| 800 Mbps | 200 | **全闭**: PLL 锁着(STATUS=0x5)、数据在流(ERR ~50%), 但门限拉到 256(≈6%)也没有一档一条 lane 干净 |

⇒ **配置 A 下可用速率 ~200M(多 lane 零误码), 400M 已基本闭, 800M 全闭。眼在 200~400M 之间急闭。**

### 🔴 lane0 是硬件问题, 不是电阻
lane0 在 200M(1UI=5ns, 别的 lane 满眼)都全相位全门限 DEAD ⇒ **不是 66Ω 均匀衰减**(那会平等地压 6 条),
是 lane0 那对差分线单独坏: **CEP1.35/36 = 球 F18/E18 = 排阻 R195 那一腿**(冷焊/开路/排线那芯)。用户要焊上查这一对。

### 逐 lane 偏斜
- 逐 lane 偏斜(不同 lane 眼位置不同)要 6 条同时零误码才需要 per-lane IDELAY 去偏斜; 测眼宽用不到。

## 🎯 2026-09-22 配置 B 实测 (电阻的影响是决定性的)

用户把**收端那块板**的 4 只排阻 (R188/R195/R196/R197) 换成 0Ω ⇒ **配置 B: 每腿 33Ω**
(发端仍 33Ω + 收端 0Ω)。换 R195 时顺手把我怀疑虚焊的那只换成新的 0Ω, **lane0 复活**
(之前配置 A 全相位全门限死, 补焊 R195 没救回 ⇒ 根因是那只排阻本身, 不是排线/座子; 上一节
"lane0 是排线 35/36 物理坏"的判断被证伪, 是收端 R195 那两条腿的问题, 换新电阻就好了)。

排线连通性 (走马灯全 48 脚) 换电阻后复测仍 48/48 全通, 0 短 0 错位。

### 眼宽-vs-速率, 配置 A(66Ω/腿) vs 配置 B(33Ω/腿), 同一根 ≤10cm 直通排线, 6 lane 全 PRBS
| 速率 | 配置 A (每腿 66Ω) | 配置 B (每腿 33Ω) |
|---|---|---|
| 200M | 4 条满眼/lane5 22%/lane0 死 | (未补测, 必然全开) |
| 400M | 基本全闭 (只 lane3 高门限勉强) | **6 条全满眼 200%UI ✅** |
| 800M | **全闭 (0 条)** | **只 lane0(200%)/lane2(65→200%) 开; lane1/3/4/5 闭** |

🎯 **结论: FS03 那 33Ω 串阻吃掉的眼宽是巨大的。** 少一只 (66→33Ω/腿) 就把 400M 从"全闭"
变成"6 条全开", 800M 从"0 条"变成"2 条"。⇒ **目标板每腿最多一只电阻 (甚至不放), 双 33Ω 绝对不行。**
800M 在 33Ω/腿 + 这套排线下仍只有 2 条开 (1 UI 只 1.25ns, 逐 lane 偏斜/SI 差异一放大就闭)。

### 🎯 配置 C (每腿全 0Ω) 完成 —— 800M 6 条全开, 台架结案

两块板 4 只排阻 (R188/195/196/197) 都换 0Ω ⇒ **配置 C: 每腿 0Ω**。扫 800M:
**6 条 lane 全部 200%UI (任意相位干净), 公共眼 eye_w=40 档=2 UI 全开, 中心 tap=20, 零误码门限下就这样。**
(公共眼第一次非 0 —— 之前都因某条 lane 死或偏斜为 0; 现在 6 条同时干净, 公共窗口铺满整个扫描区。)

### 🎯 完整结论: FS03 的 33Ω 串阻是杀死高速 LVDS 的元凶 (同一根 ≤10cm 直通排线)
| 速率 | A 每腿 66Ω | B 每腿 33Ω | C 每腿 0Ω |
|---|---|---|---|
| 400M | 全闭 | 6 条全开 | 6 条全开 |
| **800M** | **全闭 (0 条)** | 2 条 (lane0/lane2) | **✅ 6 条全开满眼零误码** |

⇒ **目标板 LVDS 対绝对不能放 33Ω 串阻 (每腿不放, 或只放极小值)。**
链路本身 / 比特序 (BITREV=0) / PLL / 4:1 ODDRx2·IDDRx2 在 800M 都成立 —— 之前配置 A/B 所有"闭"
都是那两只 33Ω 造成的, 不是 DR1 硅或 RTL 的锅。这条直接支撑 [[project_pov3d_bridge_architecture]]
与目标板 LVDS 走线的电阻取舍。

板子终态: 两块都恢复生产位流 (POVBOOT: PASS)。dr1v90 RTL 已推 github kiujkiu/dr1v90 (私有, 3 分支);
本记忆推 kiujkiu/zynq_pov。

### 板子终态 / 提交
- 两块都 `povboot.sh pl` **恢复生产位流, POVBOOT: PASS**。
- 卡上 `/mnt/mmcblk0p1/lvds_b2b/` 现在是带量具的 p50/p100/p200 tx/rx bin(全 BITREV=0 / PROBE_LANE0=0) + conn bin + `bath.sh`。
- RTL 改动分两批提交到 `feature/lvds-b2b`: 60b8cd8 (BITREV 修复), 后续一批 (THRESH 浴盆量具)。

相关: [[feedback_uplink_pll_vco_overrange_above_500]] [[feedback_every_pl_bitstream_needs_a_gp0_slave]] [[project_pov3d_fs03_cep_ground_and_third_row]]
[[project_dr1_uplink_tx_probe]] [[project_pov3d_hw_pack_dr1v90_gw5a]]
