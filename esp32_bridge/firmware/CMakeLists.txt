# Top-level ESP-IDF project file for pov_bridge.
# Requires ESP-IDF v5.3 or newer (ESP32-C5 SDIO slave support).

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(pov_bridge)
